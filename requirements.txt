# ============================================================
# Checkpoint 1: Project Foundation & Raw Dataset Audit
# ============================================================
# Only packages needed for loading and auditing the raw .xls dataset.
# No ML frameworks are included at this stage.

pandas>=2.0
xlrd>=2.0
numpy>=1.24
pytest>=7.0
